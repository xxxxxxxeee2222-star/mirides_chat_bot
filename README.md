# Telegram bot

Run with:

```bash
python bridge_bot.py
```

`config.json` must contain:

- `telegram_bot_token`
- `mirides_url`
- `online_url`

`chat` aliases:

- `chat`
- `cha`
- `ch`
- `чат`
- `чатик`
